export const MAX_DOCUMENTS_PER_USER = 50;          // 50 files per user
export const MAX_FILE_BYTES = 20 * 1024 * 1024;   // 20 MB per file
export const MAX_TOTAL_BYTES = 500 * 1024 * 1024; // 500 MB per user (10 GB bucket / 200 users)
